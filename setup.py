from setuptools import setup, find_packages

setup(
    name="tb",
    version="0.1",
    description="Handy functions to support day to day analyses",
    author="Sky Cheung",
    packages=find_packages(include=["tb"])
)